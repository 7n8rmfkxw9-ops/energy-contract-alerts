## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `npm test` passes locally (`energy-app/`)
- [ ] `npm run build` passes locally (`energy-app/`)
- [ ] Added/updated tests for any behavior change
- [ ] No secrets or `.env` values committed
